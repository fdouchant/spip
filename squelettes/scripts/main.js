jQuery(function ($) {

    // set the carousel options
    $('#quote-carousel').carousel({
        pause: true,
        interval: 6000,
    });

    // activate textillate
    $('.tlt').textillate();

});