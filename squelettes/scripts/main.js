jQuery(function ($) {

    // activate textillate
    $('.tlt').textillate();

    // activate fancybox
    $('a.fancybox').colorbox({
        'maxWidth': '75%',
        'maxHeight': '75%'
    });

});